# Detailed Template
# Includes additional information and examples

## Usage

```hcl
module "{module_name}" {{
{source_line}{version_line}
  # Required variables - these must be provided
{required_variables}
  # Optional variables - these have default values
{optional_variables}}}
```

### Required Variables

All required variables must be provided when using this module.

### Optional Variables

Optional variables have default values and can be omitted if the defaults are acceptable.
